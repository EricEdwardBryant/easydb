name: example.sqlite

update:
  cars: write.csv(cars, 'cars.csv', row.names = FALSE)

table:
  cars: cars.csv
  systems: systems.csv
  organisms:         # Table name
    - organisms      # a directory
    - organisms.csv  # relative path

keys:
  systems: system_id
  organisms: ncbi_taxonomy_id
